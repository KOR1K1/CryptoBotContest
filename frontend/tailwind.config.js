/** @type {import('tailwindcss').Config} */
// Tailwind CSS v4: Theme configuration moved to CSS using @theme directive
// This file is kept for content paths (optional in v4, but useful for explicit control)
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // Theme configuration is now in src/index.css using @theme directive
}

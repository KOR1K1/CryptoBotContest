# Single Server Optimizations

Документ описывает оптимизации, которые можно реализовать **на одном сервере** без горизонтального масштабирования.

---

## ✅ Оптимизации для Одного Сервера

### 1. WebSocket Update Throttling
**Статус**: ✅ Можно реализовать на одном сервере  
**Приоритет**: 🔴 Высокий (для 100k+ bids/round)

**Что делает**:
- Батчинг WebSocket обновлений каждые 100ms вместо немедленного emit
- Снижает нагрузку на WebSocket сервер при высоких частотах ставок
- Эмит только значимых изменений (топ-10 позиций, изменения позиции пользователя)

**Реализация**:
- Добавить `ThrottlerService` для батчинга WebSocket событий
- Использовать `setTimeout` или `setInterval` для группировки обновлений
- Хранить pending updates в памяти (Map/Set)

**Ожидаемый эффект**:
- Снижение WebSocket трафика на 70-90%
- Снижение CPU нагрузки на WebSocket сервер
- Улучшение производительности при 1,000+ bids/сек

---

### 2. Query Result Caching (Redis)
**Статус**: ✅ Можно реализовать на одном сервере (Redis локально или в Docker)  
**Приоритет**: 🟡 Средний (для 100k+ bids/round)

**Что делает**:
- Кеширование результатов dashboard queries (топ-3 ставки, позиция пользователя)
- Кеширование auction details (статус, раунды, информация о подарках)
- TTL кеша: 1-5 секунд (balance freshness)

**Реализация**:
- Добавить Redis в docker-compose (локальный Redis)
- Использовать `@nestjs/cache-manager` с Redis adapter
- Кешировать результаты `/auctions/:id/dashboard` endpoint

**Ожидаемый эффект**:
- Снижение нагрузки на MongoDB на 50-70%
- Улучшение response time для dashboard queries (<100ms)
- Снижение memory usage MongoDB

---

### 3. MongoDB Connection Pooling Optimization ✅ РЕАЛИЗОВАНО
**Статус**: ✅ Реализовано на одном сервере  
**Приоритет**: 🟢 Низкий (улучшение производительности) → ✅ **Завершено**

**Что делает**:
- Оптимизация размера connection pool MongoDB
- Настройка maxPoolSize, minPoolSize, maxIdleTimeMS
- Мониторинг активных соединений и pool usage

**Реализация**:
- ✅ Настроен `maxPoolSize` в MongooseModule (configurable через `MONGO_MAX_POOL_SIZE`, default: 50)
- ✅ Настроен `minPoolSize` (configurable через `MONGO_MIN_POOL_SIZE`, default: 5)
- ✅ Настроен `maxIdleTimeMS` (configurable через `MONGO_MAX_IDLE_TIME_MS`, default: 30000ms)
- ✅ Добавлены дополнительные настройки: `serverSelectionTimeoutMS`, `socketTimeoutMS`, `heartbeatFrequencyMS`
- ✅ Добавлен мониторинг connection pool usage в `MongoDBMetricsService`
- ✅ Добавлен API endpoint `/metrics/mongodb/pool` для мониторинга

**Конфигурация**:
- Default: maxPoolSize: 50, minPoolSize: 5, maxIdleTimeMS: 30000ms
- Production (Medium Load): maxPoolSize: 50-75, minPoolSize: 5-10
- Production (High Load): maxPoolSize: 75-100, minPoolSize: 10

**Ожидаемый эффект**:
- ✅ Улучшение response time на 10-20%
- ✅ Более эффективное использование соединений (снижение overhead на 80-90%)
- ✅ Улучшение concurrent operations handling (до 50-100 одновременных операций)

**Документация**: См. `docs/MONGODB_CONNECTION_POOL_OPTIMIZATION.md` для детального описания.

---

### 4. Frontend Polling Optimization (Page Visibility API)
**Статус**: ✅ Реализовано (frontend changes)  
**Приоритет**: 🟡 Средний (снижение нагрузки на сервер)

**Что делает**:
- **Page Visibility API**: Отключает/замедляет polling когда вкладка неактивна или браузер свернут
- Увеличение интервала polling с 2s до 5-10s при отсутствии активности
- Адаптивный polling (чаще при активности, реже при простоях)
- Использование WebSocket как primary, polling как fallback

**Реализация**:
- ✅ Использован `document.visibilitychange` API для определения активности вкладки
- ✅ Когда `document.hidden === true`: останавливаем polling полностью (или используем длинный интервал 30s)
- ✅ Когда `document.hidden === false`: возобновляем нормальный polling с немедленным обновлением
- ✅ WebSocket события работают независимо от visibility (always active)
- ✅ Таймеры также оптимизированы (не обновляются когда вкладка скрыта)

**Почему это хорошая практика**:
- ✅ **Экономия ресурсов**: Не тратим CPU, network, battery когда пользователь не видит страницу
- ✅ **Снижение нагрузки на сервер**: При 10,000 пользователей со свернутыми вкладками - экономия тысяч запросов/сек
- ✅ **Лучший UX**: Браузер не замедляется, батарея разряжается медленнее
- ✅ **Стандартная практика**: Page Visibility API поддерживается всеми современными браузерами
- ✅ **WebSocket работает независимо**: Real-time обновления через WebSocket продолжают работать

**Ожидаемый эффект**:
- Снижение HTTP запросов на 60-80% (учитывая что пользователи часто сворачивают вкладки)
- Снижение нагрузки на API сервер на 50-70%
- Улучшение battery life на мобильных устройствах на 20-30%
- Более отзывчивый браузер при множестве открытых вкладок

---

### 5. MongoDB Memory Monitoring & Tuning
**Статус**: ✅ Можно реализовать на одном сервере  
**Приоритет**: 🟡 Средний (для стабильности)

**Что делает**:
- Мониторинг использования памяти MongoDB
- Настройка WiredTiger cache size (до 50% от доступной RAM)
- Алерты при высоком использовании памяти

**Реализация**:
- Добавить мониторинг в docker-compose (mongodb-exporter или custom metrics)
- Настроить `wiredTigerCacheSizeGB` в MongoDB config
- Добавить алерты в мониторинг (например, Prometheus + Grafana)

**Ожидаемый эффект**:
- Предотвращение OOM (Out of Memory) ситуаций
- Оптимальное использование памяти
- Раннее предупреждение о проблемах

---

### 6. Bid Update Aggregation (на уровне кода)
**Статус**: ✅ Можно реализовать на одном сервере  
**Приоритет**: 🔴 Высокий (для 100k+ bids/round)

**Что делает**:
- Агрегация bid updates перед emit в WebSocket
- Батчинг обновлений: собирать все bid updates за 100ms, затем emit один раз
- Эмит только значимых изменений (топ-10 позиций)

**Реализация**:
- Создать `BidUpdateAggregator` service
- Использовать `Map<auctionId, Set<updates>>` для накопления обновлений
- Timer каждые 100ms для flush накопленных обновлений
- Эмит только если позиция в топ-10 изменилась

**Ожидаемый эффект**:
- Снижение WebSocket emits на 80-90%
- Снижение CPU нагрузки на WebSocket сервер
- Улучшение производительности при 1,000+ bids/сек

---

### 7. MongoDB Read Preferences (Single Replica Set)
**Статус**: ✅ Можно реализовать на одном сервере (MongoDB replica set локально)  
**Приоритет**: 🟢 Низкий (оптимизация, но требует setup)

**Что делает**:
- Использование read preferences для dashboard queries (secondary)
- Разделение read/write нагрузки (write на primary, read на secondary)
- Улучшение производительности при высоких read нагрузках

**Реализация**:
- Настроить MongoDB replica set (1 primary + 1 secondary на одном сервере)
- Использовать `readPreference: 'secondary'` для dashboard queries
- Write queries остаются на primary

**Ожидаемый эффект**:
- Снижение нагрузки на primary MongoDB
- Улучшение response time для read queries на 20-30%
- Лучшая производительность при concurrent reads

**Примечание**: Требует настройки MongoDB replica set, но может работать на одном сервере

---

### 8. Database Index Optimization
**Статус**: ✅ Можно реализовать на одном сервере  
**Приоритет**: 🟢 Низкий (уже оптимизировано, но можно улучшить)

**Что делает**:
- Анализ использования индексов (MongoDB explain plans)
- Оптимизация составных индексов
- Удаление неиспользуемых индексов
- Добавление покрывающих индексов (covering indexes)

**Реализация**:
- Использовать `db.collection.explain()` для анализа queries
- Анализировать slow queries (MongoDB profiler)
- Оптимизировать индексы на основе query patterns

**Ожидаемый эффект**:
- Улучшение query performance на 10-30%
- Снижение memory usage (меньше индексов)
- Более быстрая вставка данных (меньше индексов для обновления)

---

### 9. Monitoring & Alerting (Single Server)
**Статус**: ✅ Можно реализовать на одном сервере  
**Приоритет**: 🟡 Средний (критично для production)

**Что делает**:
- Мониторинг MongoDB query performance (slow queries)
- API response time monitoring (p95, p99)
- WebSocket connection count monitoring
- Memory usage monitoring (MongoDB, API server)
- Error rate monitoring

**Реализация**:
- Добавить Prometheus + Grafana (в docker-compose)
- Экспорт метрик из NestJS (prom-client)
- MongoDB exporter для MongoDB метрик
- Алерты в Grafana (email/Slack)

**Ожидаемый эффект**:
- Раннее обнаружение проблем
- Проактивное решение performance issues
- Visibility в систему

---

### 10. Response Compression
**Статус**: ✅ Можно реализовать на одном сервере  
**Приоритет**: 🟢 Низкий (улучшение network performance)

**Что делает**:
- Gzip/Brotli compression для API responses
- Снижение network traffic
- Улучшение response time для медленных соединений

**Реализация**:
- Добавить `compression` middleware в NestJS
- Настроить Gzip compression для JSON responses
- Использовать Brotli для лучшего сжатия (если поддерживается)

**Ожидаемый эффект**:
- Снижение network traffic на 60-80%
- Улучшение response time для медленных соединений
- Снижение bandwidth costs

---

## ❌ Оптимизации, Требующие Горизонтального Масштабирования

### Нельзя реализовать на одном сервере:

1. **Horizontal Scaling (Multiple API Instances)**
   - Требует load balancer и несколько серверов
   - Нельзя на одном сервере

2. **Socket.IO Redis Adapter (Multi-Server WebSocket)**
   - Требует несколько серверов для синхронизации WebSocket
   - Нельзя на одном сервере (но можно использовать Redis локально для кеша)

3. **Load Balancer Configuration**
   - Требует несколько серверов
   - Нельзя на одном сервере

4. **Database Sharding**
   - Требует несколько MongoDB instances
   - Нельзя на одном сервере (или очень сложно)

5. **Message Queue (RabbitMQ/Kafka) for Bid Processing**
   - Можно установить локально, но не даст преимуществ без нескольких потребителей
   - Не рекомендуется для одного сервера

6. **CDN for Static Assets**
   - Требует внешний CDN сервис
   - Можно использовать, но не обязательно для одного сервера

7. **Auto-Scaling Policies**
   - Требует Kubernetes или cloud provider
   - Нельзя на одном сервере

8. **Geographic Distribution**
   - Требует несколько серверов в разных регионах
   - Нельзя на одном сервере

---

## 📊 Приоритеты для Одного Сервера

### Высокий Приоритет (для 100k-1M bids/round):
1. ✅ **WebSocket Update Throttling** - Критично для производительности
2. ✅ **Bid Update Aggregation** - Снижает WebSocket нагрузку
3. ✅ **Query Result Caching (Redis)** - Снижает MongoDB нагрузку

### Средний Приоритет (улучшение производительности):
4. ✅ **Frontend Polling Optimization** - Снижает HTTP запросы
5. ✅ **Monitoring & Alerting** - Критично для production
6. ✅ **MongoDB Memory Monitoring** - Предотвращает проблемы

### Низкий Приоритет (fine-tuning):
7. ✅ **MongoDB Connection Pooling** - Улучшение производительности
8. ✅ **MongoDB Read Preferences** - Требует setup replica set
9. ✅ **Database Index Optimization** - Улучшение queries
10. ✅ **Response Compression** - Улучшение network performance

---

## 🎯 Ожидаемые Результаты (Single Server)

### При 100k bids/round (10 минут):
- ✅ **Winner Selection**: 100-200ms (уже оптимизировано)
- ✅ **Dashboard Updates**: <100ms с кешированием (вместо 200-300ms)
- ✅ **WebSocket Emits**: <10ms per batch (вместо множества отдельных emits)
- ✅ **Memory Usage**: Стабильно <8GB для MongoDB
- ✅ **Response Time**: p95 <200ms, p99 <500ms

### При 1M bids/round (10 минут):
- ⚠️ **Winner Selection**: 200-500ms (уже оптимизировано)
- ⚠️ **Dashboard Updates**: <200ms с кешированием
- ⚠️ **WebSocket Emits**: Батчинг критично важен (1,000+ bids/сек)
- ⚠️ **Memory Usage**: Требует 16GB+ RAM для MongoDB
- ⚠️ **Response Time**: p95 <500ms, p99 <1s

### Ограничения Одного Сервера:
- ⚠️ **WebSocket Connections**: Максимум ~10,000-20,000 одновременных подключений
- ⚠️ **CPU**: Может стать узким местом при 1,000+ bids/сек
- ⚠️ **Memory**: Требует 16GB+ RAM для 1M+ bids/round
- ⚠️ **Network**: Может стать узким местом при высоких WebSocket частотах

---

## 📝 Рекомендации

### Для MVP / Small Scale (10k-100k bids/round):
✅ **Уже достаточно оптимизаций** - текущая реализация отлично работает

### Для Medium Scale (100k-1M bids/round):
✅ **Рекомендуется**:
1. WebSocket Update Throttling (🔴 Высокий приоритет)
2. Query Result Caching (Redis) (🔴 Высокий приоритет)
3. Bid Update Aggregation (🔴 Высокий приоритет)
4. Monitoring & Alerting (🟡 Средний приоритет)
5. Frontend Polling Optimization (🟡 Средний приоритет)

### Для Large Scale (1M+ bids/round):
⚠️ **Один сервер может не справиться** - требуется горизонтальное масштабирование

---

## ✅ Вывод

**На одном сервере можно реализовать 7-8 из 10 оптимизаций**, что достаточно для **100k-1M bids/round**.

**Критические оптимизации для одного сервера**:
1. WebSocket Update Throttling
2. Query Result Caching (Redis)
3. Bid Update Aggregation
4. Monitoring & Alerting

**Эти оптимизации позволят системе эффективно работать на одном сервере до 1M bids/round** (10 минут раунд, ~1,667 bids/сек средняя нагрузка).
